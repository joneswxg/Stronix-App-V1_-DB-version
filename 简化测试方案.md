# 简化测试方案 - 专门解决删除操作问题

## 问题现状
根据用户测试：
1. ✅ **编辑计划然后返回** - 正常，没有触发页面跳转
2. ❌ **删除计划** - 触发了页面跳转到"体测"页面

## 根本原因
删除操作调用了 `PlanViewModel.deletePlan()`，该方法之前会触发 `loadPersonalPlans()`，导致大量API调用：
- 获取个人计划列表
- 为每个计划获取详情（从日志可以看到多个 `/plans/X` 调用）

这些大量的 `@Published` 属性更新导致 SwiftUI 重建 `MainTabView`。

## 解决方案
**完全避免API调用** - 直接操作本地数据：

### 修改前（问题代码）:
```swift
func deletePlan(_ plan: TrainingPlan) async {
    try await planService.deletePlan(planId: plan.id)
    await loadPersonalPlans() // 🚫 触发大量API调用
}
```

### 修改后（解决方案）:
```swift
func deletePlan(_ plan: TrainingPlan) async {
    try await planService.deletePlan(planId: plan.id)
    personalPlans.removeAll { $0.id == plan.id } // ✅ 只操作本地数据
}
```

## 测试步骤

### 测试删除操作
1. **操作步骤**:
   - 确保在"训练"标签页（selectedTab = 2）
   - 选择一个个人计划
   - 点击删除按钮
   - 确认删除

2. **预期结果**:
   - 计划从列表中消失
   - **不应该**跳转到"体测"页面
   - 停留在"训练"标签页

3. **观察日志**:
   ```
   🌐 API响应 [/plans/X]: 删除成功
   🔄 PlanViewModel.deletePlan() 本地删除完成，剩余 Y 个计划
   成功: 计划已删除
   ```
   
   **不应该看到**:
   - 大量的 `/plans/personal` 和 `/plans/X` API调用
   - `🔄 MainTabView onAppear - selectedTab: 0`

### 测试复制模板操作
1. **操作步骤**:
   - 在"计划模版"标签中选择一个模板
   - 点击"使用"按钮
   - 确认复制

2. **预期结果**:
   - 新计划出现在"个人计划"列表顶部
   - **不应该**跳转到"体测"页面

3. **观察日志**:
   ```
   🌐 API响应 [/plans/copy/X]: 复制成功
   🔄 PlanViewModel.copyTemplatePlan() 本地添加完成，现有 Y 个计划
   成功: 已将模板计划复制到个人计划
   ```

## 成功标准
- ✅ 删除计划后停留在"训练"标签页
- ✅ 复制模板后停留在"训练"标签页
- ✅ 控制台没有出现 `MainTabView onAppear - selectedTab: 0`
- ✅ 没有大量不必要的API调用
- ✅ 数据更新正确反映在UI中

## 如果问题仍然存在
如果修改后仍然出现跳转，请检查：

1. **其他可能的触发点**:
   - 是否有其他 `ObservableObject` 在删除操作后被更新
   - 是否有其他视图在监听 `PlanViewModel` 的变化

2. **自动恢复机制**:
   - 如果看到跳转，应该立即看到自动恢复日志：
     ```
     ⚠️ MainTabView 意外跳转到体测页面！之前在: 2，用户最后选择: 2
     🔄 MainTabView 恢复到用户最后选择的标签页: 2
     ```

3. **完全禁用自动刷新**:
   - 如果问题持续，可以暂时注释掉所有自动刷新逻辑
   - 只依赖手动下拉刷新来更新数据

## 优势
这个解决方案的优势：
1. **性能更好** - 避免不必要的网络请求
2. **用户体验更好** - 立即响应，无需等待网络
3. **更稳定** - 减少状态更新，降低视图重建风险
4. **数据一致性** - 本地操作与服务器操作同步进行 