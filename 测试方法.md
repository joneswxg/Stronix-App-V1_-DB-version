# 测试方法 - 修复数据库操作后页面自动跳转问题

## 问题描述
在进行数据库操作（创建计划、编辑计划）后，应用会自动跳转到"体测"页面（MainTabView 的默认页面），而不是停留在原来的页面。

## 修改内容总结

### 1. EditPlanView.swift 修改
- **修改位置**: `savePlan()` 方法中的成功处理逻辑
- **修改内容**: 
  - 移除了 `showPlanDetail = true` 的逻辑
  - 改为调用 `onSaveSuccess?()` 回调，让父视图处理后续逻辑
- **目的**: 避免在 EditPlanView 即将关闭时弹出新的 sheet，减少视图状态冲突

### 2. CreatePlanView.swift 修改
- **修改位置**: `savePlan()` 方法中的成功处理逻辑
- **修改内容**: 
  - 在成功后延迟 1 秒自动调用 `dismiss()`
  - 移除了 alert 中的 `dismiss()` 调用
- **目的**: 让成功提示显示完毕后再关闭视图，避免状态冲突

### 3. PlanListView.swift 修改
- **修改位置**: 
  - `CreatePlanView` 的 `onDisappear` 回调
  - `EditPlanView` 的 `onSaveSuccess` 回调
- **修改内容**: 
  - 添加 0.1 秒延迟后再调用 `viewModel.refresh()`
  - 确保模态视图状态先设为 false
- **目的**: 让视图层级稳定后再刷新数据，避免在视图转换过程中触发状态更新

### 4. MainTabView.swift 修改
- **修改位置**: 添加调试日志和状态保护机制
- **修改内容**: 
  - 添加 `onChange(of: selectedTab)` 监听器
  - 添加 `onAppear` 日志
  - 添加 `lastUserSelectedTab` 状态记录用户最后选择的标签页
  - 当检测到意外跳转到体测页面时，自动恢复到用户最后选择的标签页
- **目的**: 跟踪 selectedTab 的变化，并提供自动恢复机制

### 5. PlanViewModel.swift 修改
- **修改位置**: `refresh()` 方法和所有数据库写操作方法
- **修改内容**: 
  - 将 `refresh()` 改为只刷新个人计划列表，不重新加载模板计划
  - 添加 `refreshPersonalPlansOnly()` 方法，减少API调用
  - **关键修改**: `deletePlan()` 改为直接从本地数组删除，完全避免API调用
  - **关键修改**: `copyTemplatePlan()` 改为直接在本地数组添加新计划，避免API调用
  - `updatePlan()` 也改为使用轻量级刷新
- **目的**: 彻底避免数据库写操作后的大量API调用，防止视图重建

## 测试步骤

### 测试前准备
1. 确保后端服务器正在运行（端口 6000）
2. 确保应用已登录
3. 打开 Xcode 控制台查看日志输出

### 测试场景 1: 创建新计划
1. **操作步骤**:
   - 启动应用
   - 切换到"训练"标签页（确保 selectedTab = 2）
   - 点击"个人计划"标签
   - 点击右上角的"+"按钮创建新计划
   - 输入计划名称
   - 添加至少一个动作和组数
   - 点击"保存"

2. **预期结果**:
   - 显示"计划保存成功！"提示
   - 1秒后自动关闭创建页面
   - 返回到"训练"标签页的"个人计划"列表
   - **不应该**跳转到"体测"页面

3. **观察日志**:
   ```
   🔄 CreatePlanView - 保存成功，准备关闭视图
   🔄 CreatePlanView onDisappear - 延迟刷新数据
   🔄 PlanViewModel.refreshPersonalPlansOnly() 开始
   🔄 PlanViewModel.refreshPersonalPlansOnly() 完成，加载了 X 个计划
   🔄 MainTabView selectedTab 变化: X -> Y (应该保持在 2)
   ```
   
   **如果出现意外跳转，应该看到**:
   ```
   ⚠️ MainTabView 意外跳转到体测页面！之前在: 2，用户最后选择: 2
   🔄 MainTabView 恢复到用户最后选择的标签页: 2
   ```

### 测试场景 2: 编辑现有计划
1. **操作步骤**:
   - 确保在"训练"标签页（selectedTab = 2）
   - 在"个人计划"列表中选择一个计划
   - 点击"编辑"按钮
   - 修改计划内容（如修改动作的重量或次数）
   - 点击"保存"

2. **预期结果**:
   - 显示"保存成功！"提示
   - 1秒后触发 onSaveSuccess 回调
   - 自动关闭编辑页面
   - 返回到"训练"标签页的"个人计划"列表
   - **不应该**跳转到"体测"页面

3. **观察日志**:
   ```
   🔄 EditPlanView onSaveSuccess 回调被触发
   🔄 EditPlanView onSaveSuccess - 延迟刷新数据
   🔄 PlanViewModel.refreshPersonalPlansOnly() 开始
   🔄 PlanViewModel.refreshPersonalPlansOnly() 完成，加载了 X 个计划
   🔍 EditPlanView onDisappear - 不触发刷新
   🔄 MainTabView selectedTab 变化: X -> Y (应该保持在 2)
   ```

### 测试场景 3: 验证其他标签页操作
1. **操作步骤**:
   - 切换到"动作"标签页（selectedTab = 1）
   - 执行一些操作（如查看动作详情）
   - 切换到"历史"标签页（selectedTab = 3）
   - 查看训练历史

2. **预期结果**:
   - 标签页切换正常
   - 不会意外跳转到"体测"页面

### 测试场景 4: 压力测试
1. **操作步骤**:
   - 快速连续创建多个计划
   - 快速连续编辑多个计划
   - 在保存过程中尝试切换标签页

2. **预期结果**:
   - 所有操作都应该正常完成
   - 不应该出现页面跳转问题

## 问题诊断

### 如果问题仍然存在
查看控制台日志，重点关注：

1. **MainTabView 日志**:
   ```
   ⚠️ MainTabView 意外跳转到体测页面！之前在: X
   ```
   如果看到这个日志，说明 selectedTab 确实被重置了

2. **时序问题**:
   观察日志的时间顺序，看是否在视图关闭和数据刷新之间有冲突

3. **视图重建**:
   如果看到多次 `MainTabView onAppear`，说明 MainTabView 被重新创建了

### 进一步调试
如果问题持续，可以：

1. **增加更多延迟**:
   将 0.5 秒延迟增加到 1.0 秒或更长

2. **检查其他 ObservableObject**:
   查看是否有其他全局状态管理器在数据库操作后被更新

3. **简化测试**:
   暂时注释掉 `viewModel.refresh()` 调用，看是否还会跳转

4. **完全禁用刷新**:
   如果自动恢复机制工作正常，可以考虑暂时禁用数据刷新，只在用户手动下拉刷新时更新数据

## 成功标准
- ✅ 创建计划后停留在"训练"标签页
- ✅ 编辑计划后停留在"训练"标签页  
- ✅ 控制台没有出现意外的 selectedTab 变化日志
- ✅ 数据正常刷新，新创建/编辑的计划能正确显示
- ✅ 其他功能不受影响

## 回滚方案
如果修改导致其他问题，可以：
1. 恢复 EditPlanView 中的 `showPlanDetail = true` 逻辑
2. 恢复 CreatePlanView 中 alert 的 `dismiss()` 调用
3. 移除 PlanListView 中的延迟逻辑
4. 移除 MainTabView 中的调试日志 